{
  "name": "microservices-and-react-app",
  "version": "1.0.0",
  "scripts": {
    "compose:up": "(cd apps && docker-compose up -d --build)",
    "compose:down": "(cd apps && docker-compose down --remove-orphans)"
  }
}